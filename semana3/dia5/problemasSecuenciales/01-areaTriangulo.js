/**
 * Realice el algoritmo para obtener el área de un triángulo.
 */

let base = +prompt("Ingrese base de triángulo: ");
let altura = +prompt("Ingrese altura de triángulo: ");
area = base * altura;
alert(`El área del triángulo e: ${area}`);