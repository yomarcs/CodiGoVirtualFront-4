/**
 * Realice el algoritmo para obtener el área de un triángulo.
 */

debugger; 

let base = +prompt("Area triángulo.\nIngrese base de triángulo: ");
let altura = +prompt("Area triángulo.\nIngrese altura de triángulo: ");
area = (base * altura) / 2;
alert(`Area triángulo.\nEl área del triángulo e: ${area}`);